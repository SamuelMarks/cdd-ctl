use log::*;

fn main() {
    let result = cdd::run();
    // match result {
    //     Ok(_) => (),
    //     Err(err) => {
    //         for line in format!("{}", err).lines() {
    //             error!("{}", line)
    //         }
    //     }
    // };
}
